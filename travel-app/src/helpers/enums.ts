export enum TitleTypes {
  primary,
  secondary,
}

export enum CardTypes {
  city,
  hotel,
  activity,
  text,
}

export enum SearchModalSettingsTypes {
  destination = 0,
  guests = 1,
  date = 2,
  filters = 3,
}
